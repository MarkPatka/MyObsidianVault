5 минут чтения · 6 декабря 2025 г.

Итак, вы пишете код стандартным способом:
```csharp
var orders = await GetRecentOrdersAsync(userId);
var logs   = await GetSystemLogsAsync();
var stats  = await GetUserStatsAsync(userId);
return new DashboardDto(orders, logs, stats);
```
Это работает. Это чисто. Это читаемо. Но есть проблема.

Если `GetRecentOrdersAsync` занимает 300 мс, `GetSystemLogsAsync` — 400 мс, а `GetUserStatsAsync` — 300 мс, то ваши пользователи смотрят на индикатор загрузки целую секунду (300 + 400 + 300).

В распределённых системах задержка убивает пользовательский опыт. Поскольку эти наборы данных не связаны между собой, мы должны иметь возможность выполнять их параллельно. Если бы мы это сделали, общее время составило бы только длительность самого медленного запроса (400 мс). Это улучшение производительности на 60%, достигнутое просто изменением способа выполнения кода.

Но если вы попробуете наивный подход с Entity Framework Core, ваше приложение упадёт.

## Ложное Обещание `Task.WhenAll`

Самая распространённая ошибка, которую совершают разработчики, пытаясь оптимизировать это, — оборачивание своих существующих вызовов репозитория в задачи и ожидание их всех одновременно.

**Это выглядит примерно так:**
```csharp
public async Task<DashboardData> GetDashboardData(int userId)
{
    // Эти методы используют один и тот же _dbContext
    var ordersTask = _repository.GetOrdersAsync(userId);
    var logsTask   = _repository.GetLogsAsync();
    var statsTask  = _repository.GetStatsAsync(userId);

    await Task.WhenAll(ordersTask, logsTask, statsTask); // ERROR 💥

    return new DashboardData(ordersTask.Result, logsTask.Result, statsTask.Result);
}
```

Если вы запустите это, вы сразу же получите исключение:
> "A second operation started on this context before a previous operation completed. This is usually caused by different threads using the same instance of DbContext, however instance members are not guaranteed to be thread safe."

### Проблема 

**`DbContext` в EF Core не является потокобезопасным**. 
Это объект с состоянием, предназначенный для управления одной единицей работы. Он поддерживает "Отслеживание изменений" (Change Tracker) для отслеживания загруженных сущностей и оборачивает одно базовое соединение с базой данных.

Протоколы баз данных (например, TCP-поток для PostgreSQL или SQL Server) обычно синхронны на уровне соединения. Вы не можете отправить два разных SQL-запроса по одному и тому же каналу в точно одинаковую миллисекунду. Когда вы используете `Task.WhenAll`, несколько потоков пытаются одновременно захватить это единственное соединение, и EF Core вмешивается, чтобы выбросить исключение и предотвратить повреждение данных.

Таким образом, у нас дилемма: мы хотим скорости параллелизма, но `DbContext` вынуждает нас к последовательному выполнению.

### Решение

Начиная с .NET 5, EF Core предоставляет первоклассное решение именно для этого сценария: `IDbContextFactory<T>`.

Вместо внедрения экземпляра вашего контекста с областью видимости (scoped) (который живет в течение всего HTTP-запроса), вы внедряете фабрику, которая позволяет создавать легковесные, независимые экземпляры `DbContext` по требованию.

_Примечание: Хотя использование фабрики — самый чистый подход для внедрения зависимостей, вы также можете вручную создавать экземпляр контекста (используя `var context = new AppDbContext(options)`), если у вас есть доступ к `DbContextOptions`._

Сначала нам нужно зарегистрировать фабрику в нашем `Program.cs`.
```csharp

// Это регистрирует IDbContextFactory<AppDbContext> как Singleton (по умолчанию)
// А также регистрирует AppDbContext как Scoped для удобства использования в других местах
builder.Services.AddDbContextFactory<AppDbContext>(options =>
{
    options.UseNpgsql(builder.Configuration.GetConnectionString("db"));
});
```

Теперь вместо внедрения `AppDbContext` мы внедряем `IDbContextFactory<AppDbContext>`.

Внутри нашего метода мы запускаем отдельную задачу для каждого запроса. Внутри каждой задачи мы создаем совершенно новый контекст, выполняем запрос и тут же освобождаем его.

```csharp
using Microsoft.EntityFrameworkCore;

public class DashboardService(IDbContextFactory<AppDbContext> contextFactory)
{
    public async Task<DashboardDto> GetDashboardAsync(int userId)
    {
        // 1. Запускаем задачи (Запросы начинают выполняться сразу при вызове)
        var ordersTask = GetOrdersAsync(userId);
        var logsTask = GetSystemLogsAsync();
        var statsTask = GetUserStatsAsync(userId);

        // 2. Ждём завершения всех
        await Task.WhenAll(ordersTask, logsTask, statsTask);

        // 3. Возвращаем результаты (использование 'await Task.WhenAll' здесь чисто извлекает результат)
        return new DashboardDto(
            await ordersTask,
            await logsTask,
            await statsTask
        );
    }

    private async Task<List<Order>> GetOrdersAsync(int userId)
    {
        // Создаём свежий контекст для этой конкретной операции
        await using var context = await contextFactory.CreateDbContextAsync();

        return await context.Orders
            .AsNoTracking()
            .Where(o => o.UserId == userId)
            .OrderByDescending(o => o.CreatedAt)
            .ThenByDescending(o => o.Amount)
            .Take(50)
            .ToListAsync();
    }

    private async Task<List<SystemLog>> GetSystemLogsAsync()
    {
        await using var context = await contextFactory.CreateDbContextAsync();

        return await context.SystemLogs
            .AsNoTracking()
            .OrderByDescending(l => l.Timestamp)
            .Take(50)
            .ToListAsync();
    }

    private async Task<UserStats?> GetUserStatsAsync(int userId)
    {
        await using var context = await contextFactory.CreateDbContextAsync();

        return await context.Users
            .Where(u => u.Id == userId)
            .Select(u => new UserStats { OrderCount = u.Orders.Count })
            .FirstOrDefaultAsync();
    }
}
```
### Ключевые Концепции

- **Изоляция**: Каждая задача получает свой собственный `DbContext`. Это означает, что они получают собственное соединение с базой данных. Нет конкуренции.

- **Освобождение ресурсов**: Обратите внимание на `await using`. Это критически важно. Как только запрос выполнен, мы хотим освободить этот контекст и вернуть соединение в пул.
### Бенчмарк

Чтобы доказать, что это работает, я создал небольшое приложение .NET 10, используя Aspire и PostgreSQL. Поскольку я запускаю его локально, абсолютные времена очень низкие. Если бы я использовал удалённую базу данных, времена были бы выше, но коэффициент ускорения был бы похожим.

**Последовательное выполнение: ~36 мс**  
![[EF Core последовательное выполнение..png]]
Каждая операция ждёт завершения предыдущей.

**Параллельное выполнение: ~13 мс** 
![[EFCore параллельное выполнение.png]]
При использовании параллельного подхода временная шкала сжимается. Все три обращения к базе данных начинаются одновременно и завершаются вместе.

### Компромиссы и Заключение

`IDbContextFactory` служит мостом между подходом EF Core к единице работы и реалиями современных, параллельных требований. Он позволяет вырваться из рамок "один запрос, один поток", не жертвуя безопасностью.

Однако используйте этот паттерн с умом:

- **Истощение пула соединений**: Один HTTP-запрос теперь занимает 3 соединения с базой данных одновременно вместо 1. При высокой конкурентности вы легко можете исчерпать пул соединений.

- **Накладные расходы контекста**: Если ваши запросы очень быстрые (например, простые поиски по ID), накладные расходы на создание нескольких контекстов и задач могут сделать параллельную версию медленнее, чем последовательную.

Не спешите сразу переходить на чистый SQL. Проверьте ваши `await`. Если они выстроены в линию, возможно, пришло время добавить немного параллелизма.